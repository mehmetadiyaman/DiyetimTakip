import theme from './theme';
import palette from './colors';
import { typography, textVariants } from './typography';
import { shape, shadows, spacing } from './spacing';
import animation from './animation';
import { buttons, inputs, cards } from './components';
import commonStyles from './commonStyles';

export {
  theme as default,
  palette,
  typography,
  textVariants,
  shape,
  shadows,
  spacing,
  animation,
  buttons,
  inputs,
  cards,
  commonStyles
}; 